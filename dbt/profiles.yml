pipeline_clima:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: airflow
      password: airflow
      dbname: datalake
      schema: analytics
